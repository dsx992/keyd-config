# config for keyd

- uses caps as second layer
- multimedia keys
- vim arrow bindings
- 60% keeb friendly

# prerequisites

- keyd
- keyboard

# install

- run ```./install.sh```
    - creates symlink to ```/etc/Keyd```
    - enables the daemon
    - starts the daemon
    - reloads the config
